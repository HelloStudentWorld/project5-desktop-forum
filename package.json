{
  "name": "forum-app",
  "version": "1.0.0",
  "description": "Forum Application with React frontend and Express backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../backend && npm install",
    "dev:client": "cd client && npm start",
    "dev:server": "cd backend && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": "18.x"
  },
  "cacheDirectories": [
    "client/node_modules",
    "backend/node_modules"
  ]
}